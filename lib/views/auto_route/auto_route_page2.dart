import 'package:flutter/material.dart';

class AutoRoutePage2 extends StatelessWidget {
  const AutoRoutePage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AutoRoutePage2'),
      ),
      body: const Text('AutoRoutePage2'),
    );
  }
}
