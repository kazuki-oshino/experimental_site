import 'package:flutter/material.dart';

class AutoRoutePage3 extends StatelessWidget {
  const AutoRoutePage3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AutoRoutePage3'),
      ),
      body: const Text('AutoRoutePage3'),
    );
  }
}
