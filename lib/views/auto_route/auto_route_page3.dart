import 'package:flutter/material.dart';

class AutoRoutePage3 extends StatelessWidget {
  const AutoRoutePage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AutoRoutePage3'),),
      body: Text('AutoRoutePage3'),
    );
  }
}
