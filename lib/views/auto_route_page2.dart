import 'package:flutter/material.dart';

class AutoRoutePage2 extends StatelessWidget {
  const AutoRoutePage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AutoRoutePage2'),),
      body: Text('AutoRoutePage2'),
    );
  }
}
